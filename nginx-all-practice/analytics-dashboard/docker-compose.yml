services:
  backend:
    build: ./py
    environment:
      DB_NAME: postgres 
      DB_USER: postgres
      DB_PASS: 55%+A%)6v86(FRyc9e890
      DB_HOST: host.docker.internal
      DB_PORT: 5432
    restart: on-failure
    ports:
      - "8080:5000"

  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - backend
